[package]
name = "machfab-cartridge-sdk"
version = "0.100.520"
edition = "2021"
authors = ["https://github.com/machinefabric"]
description = "Cartridge SDK for MachFab document processing tools"
license = "MIT"

[dependencies]
capdag = { path = "../capdag" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = []
